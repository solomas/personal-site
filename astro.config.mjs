// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // The production address. Canonical links, the sitemap and the social
  // preview tags build absolute URLs from it.
  site: 'https://tomasvangorp.com',
});
